local ParticipantAdded = require(script.ParticipantAdded)
local ParticipantRemoved = require(script.ParticipantRemoved)
local ParticipantWasMuted = require(script.ParticipantWasMuted)
local ParticipantWasUnmuted = require(script.ParticipantWasUnmuted)
local VoiceChangerWasEnabled = require(script.VoiceChangerWasEnabled)
local VoiceChangerWasDisabled = require(script.VoiceChangerWasDisabled)
local VoicePackWasSet = require(script.VoicePackWasSet)
local VoiceChangerFeedbackWasEnabled = require(script.VoiceChangerFeedbackWasEnabled)
local VoiceChangerFeedbackWasDisabled = require(script.VoiceChangerFeedbackWasDisabled)
local ParticipantIsActive = require(script.ParticipantIsActive)
local ParticipantIsInactive = require(script.ParticipantIsInactive)
local ExperienceLeft = require(script.ExperienceLeft)
local ExperienceJoined = require(script.ExperienceJoined)
local VoiceStatusChanged = require(script.VoiceStatusChanged)
local RestoreState = require(script.RestoreState)
local ParticipantAudioFocusChanged = require(script.ParticipantAudioFocusChanged)
local MuteParticipant = require(script.MuteParticipant)
local UnmuteParticipant = require(script.UnmuteParticipant)

return {
	ParticipantAdded = ParticipantAdded,
	ParticipantRemoved = ParticipantRemoved,
	ParticipantWasMuted = ParticipantWasMuted,
	ParticipantWasUnmuted = ParticipantWasUnmuted,
	VoiceChangerWasEnabled = VoiceChangerWasEnabled,
	VoiceChangerWasDisabled = VoiceChangerWasDisabled,
	VoicePackWasSet = VoicePackWasSet,
	VoiceChangerFeedbackWasEnabled = VoiceChangerFeedbackWasEnabled,
	VoiceChangerFeedbackWasDisabled = VoiceChangerFeedbackWasDisabled,
	ParticipantIsActive = ParticipantIsActive,
	ParticipantIsInactive = ParticipantIsInactive,
	ExperienceLeft = ExperienceLeft,
	ExperienceJoined = ExperienceJoined,
	VoiceStatusChanged = VoiceStatusChanged,
	RestoreState = RestoreState,
	ParticipantAudioFocusChanged = ParticipantAudioFocusChanged,
	MuteParticipant = MuteParticipant,
	UnmuteParticipant = UnmuteParticipant
}
