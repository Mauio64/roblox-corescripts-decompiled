local getCameraOptIn = require(script.getCameraOptIn)
local getCameraDevicePermission = require(script.getCameraDevicePermission)

return {
	getCameraOptIn = getCameraOptIn,
	getCameraDevicePermission = getCameraDevicePermission
}
