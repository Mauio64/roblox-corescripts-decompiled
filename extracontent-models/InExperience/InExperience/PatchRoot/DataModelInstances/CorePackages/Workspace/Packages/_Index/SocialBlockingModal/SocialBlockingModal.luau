return {
	["jest.config"] = script["jest.config"],
	BlockingModal = require(script.BlockingModal),
	installReducer = require(script.Rodux.installReducer),
	RoduxKey = require(script.Rodux.RODUX_KEY)
}
