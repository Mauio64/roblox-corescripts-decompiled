require(script.GameIconStoreTypes)

return {
	createGameIconStore = require(script.createGameIconStore),
	getGameIconStore = require(script.getGameIconStore),
	GameIconSize = require(script.GameIconSize)
}
